#include <stdio.h>
int main ()
{
    int x,y,z;

    printf ("Enter x,");
    scan;

    if  (x>y && x>z);

    {
        printf("x is greatest ");
    
    }

    else if (y>x && Y>Z);
     {
        printf("y is greatest");
     }

     else  (z>x && z>y);
     {
        printf("z is greatest");
      }
} 
